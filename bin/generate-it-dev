#!/bin/bash

GID_VERSION=${GID_VERSION:-build}

if [ ! -f $HOME/source/acrontum/github/generate-it/${GID_VERSION}/cli.js ]; then
  cd $HOME/source/acrontum/github/generate-it/ && npm run ${GID_VERSION}
fi
node $HOME/source/acrontum/github/generate-it/${GID_VERSION}/cli.js $*
