#!/bin/bash

declare -A commands=(\
  [sublime]=sub \
  [chrome]=google \
)

declare -A defaulted=(\
  [slack]=false \
)


if [ -z "$(pgrep $1)" ] || ! ${defaulted[${1-false}]}; then
  if ! [ -z ${commands[$1]} ]; then
    ${commands[$1]}
  else
    $1
  fi
else
  bf $1
fi
